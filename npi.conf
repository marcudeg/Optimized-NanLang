# npi.conf — Monorepo root config

lib_dir     = ./lib
cache_dir   = ./.npi_cache
registry_url = https://registry.nanlang.bluearch.network
compiler    = /usr/bin/nanlang_cpp
build_flags = --opt
output_dir  = .
